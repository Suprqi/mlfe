# مَلَفّي — وثيقة التسليم

تطبيق ويب عربي لمعلّمي المملكة: يبني ملف الإنجاز، ويحضّر الدروس، ويولّد
الوثائق المدرسية، ويصدّرها PDF أو يطبعها.

- **الموقع الحيّ:** https://mlfe.vercel.app
- **المستودع:** https://github.com/Suprqi/mlfe
- **الاستضافة:** Vercel (نشر تلقائي من فرع `main`)
- **لا خطوة بناء ولا حزم:** ارفع الملفات كما هي وتعمل.

---

## ١) بنية الملفات

### التطبيق والصفحات

| الملف | ما هو |
|---|---|
| `app.html` | **التطبيق كله** — ٢٫٨ ميغابايت، ملف واحد يحوي البنية والتنسيق والشيفرة والخطوط. يُخدَم على `/app` |
| `app-v25.html` | البناء السابق، مُبقًى على `/app-v25` للرجوع إليه، ومحجوب عن محرّكات البحث عبر `vercel.json` |
| `index.html` | صفحة الهبوط على `/` |
| `journey.html` | صفحة «الرحلة» |
| `gallery.html` | معرض اللقطات، يعرض ما في `shots/` |
| `awards.html` | صفحة الجائزة |
| `about.html` · `contact.html` | عن المنصّة، والتواصل |
| `privacy.html` · `terms.html` · `refund.html` | الخصوصية، الشروط، الاسترجاع — **صفحات نظامية، لا تُعدَّل بلا مراجعة** |
| `shots/01..10-*.png` | عشر لقطات للمعرض |

### الدوال الخادمية (Vercel Functions)

| الملف | ما تفعله | يحتاج |
|---|---|---|
| `api/chat.js` | وسيط لنموذج Claude: يستقبل من المتصفّح ويضيف المفتاح خادميًّا | `ANTHROPIC_API_KEY` |
| `api/vision.js` | وسيط تحليل الصور (تصنيف الشواهد، قراءة الجداول) | `ANTHROPIC_API_KEY` |
| `api/_auth.js` | وحدة مشتركة: تتحقّق من جلسة المستخدم قبل السماح باستدعاء الذكاء | `SUPABASE_URL` · `SUPABASE_ANON_KEY` |
| `api/subscribe.js` | ينشئ فاتورة في مويسر ويعيد رابط الدفع | `MOYASAR_SECRET_KEY` |
| `api/moyasar-webhook.js` | يفعّل الاشتراك بعد تأكيد الدفع | `MOYASAR_WEBHOOK_TOKEN` · `SUPABASE_SERVICE_ROLE_KEY` |
| `api/delete-account.js` | حذف نهائي: البيانات والاشتراك وحساب المصادقة | `SUPABASE_SERVICE_ROLE_KEY` |
| `api/award-submit.js` | استقبال مشاركات الجائزة بتحكيم مُعمّى | `AWARD_OPEN` · `AWARD_CLOSES_AT` |

### البنية التحتية

| الملف | ما هو |
|---|---|
| `vercel.json` | `cleanUrls` (فـ`/app` بلا `.html`)، ومهلة ٦٠ ثانية لدالّتَي الذكاء، وحجب `/app-v25` عن الفهرسة |
| `package.json` | تعريف فقط — لا تبعيّات. Node ١٨ فأعلى |
| `supabase/schema.sql` | مخطّط قاعدة البيانات كاملًا. يُشغَّل **مرّة واحدة** |
| `migration/` | حزمة ترحيل الاستضافة إلى مركز بيانات SCCC بالرياض (خمسة ملفات، اقرأ `migration/README.md`) |
| `.gitignore` | يستبعد `.claude/` · `node_modules/` · `.vercel/` · `fonts_block.css` |

---

## ٢) كيف يعمل

**التطبيق ملف واحد بلا إطار عمل.** الحالة كلها في كائن واحد اسمه `S`،
يُحفظ في `localStorage` تحت المفتاح `malaffi_state_v1`. لا قاعدة بيانات
محليّة ولا حالة موزّعة — كائن واحد يُقرأ ويُكتب.

**المزامنة** عبر Supabase: جدول `app_state` يحمل صفًّا واحدًا لكل معلّم
يخزّن الكائن `S` كما هو. مفتاح `anon` مكتوب داخل `app.html` — وهذا
مقصود وآمن: المفتاح عام بطبيعته، والحماية الحقيقية من **RLS** الذي
يمنع كل معلّم من رؤية صفّ غيره.

**الذكاء الاصطناعي** لا يُستدعى من المتصفّح مباشرة أبدًا. الطلب يمرّ
بـ`/api/chat` أو `/api/vision` حيث يُضاف المفتاح خادميًّا — وإلّا لَقرأ
أيُّ زائر المفتاح من الشيفرة.

**الخطوط مضمَّنة** داخل `app.html` بصيغة base64: ست عشرة كتلة
`@font-face` لخطّ IBM Plex Sans Arabic بشرائحه العربية واللاتينية. **لا
تعتمد على Google Fonts ولا على أي مصدر خطوط خارجي** — أُزيلت عمدًا لأن
الطباعة كانت تلتقط الصفحة قبل وصول الخطّ فتخرج الحروف متداخلة.

**تصدير PDF** عبر `jsPDF` و`html2canvas` تُحمَّلان عند الطلب من cdnjs.
استيراد فهرس الكتاب يستعمل `pdf.js` من المصدر نفسه.

---

## ٣) الرفع من الصفر

1. **استورد المستودع في Vercel** — لا إعدادات بناء، اتركها فارغة.
2. **أضف متغيّرات البيئة** (الجدول أدناه) من
   `Project → Settings → Environment Variables`.
3. **شغّل المخطّط**: افتح Supabase ← `SQL Editor` ← `New query`، الصق
   `supabase/schema.sql` كاملًا ثم `Run`. مرّة واحدة فقط.
4. **اربط النطاق** من `Project → Settings → Domains`.
5. **مويسر**: وجّه الـwebhook إلى `https://<نطاقك>/api/moyasar-webhook`.

---

## ٤) متغيّرات البيئة

| المتغيّر | أين يُستعمل | سرّي؟ |
|---|---|---|
| `ANTHROPIC_API_KEY` | `chat` · `vision` | **نعم — سرّي** |
| `SUPABASE_URL` | `_auth` وبقيّة الدوال | لا |
| `SUPABASE_ANON_KEY` | `_auth` | لا (عام بطبيعته، محميّ بـRLS) |
| `SUPABASE_SERVICE_ROLE_KEY` | `moyasar-webhook` · `delete-account` | **نعم — يتجاوز RLS كليًّا** |
| `MOYASAR_SECRET_KEY` | `subscribe` | **نعم — سرّي** |
| `MOYASAR_WEBHOOK_TOKEN` | `moyasar-webhook` | **نعم — سرّي** |
| `ENFORCE_SUBSCRIPTION` | بوّابة الاشتراك | لا |
| `AWARD_OPEN` | فتح باب الجائزة | لا |
| `AWARD_CLOSES_AT` | موعد الإغلاق (ISO) | لا |

> **لا تكتب أيًّا من المفاتيح السرّية في ملف داخل المستودع.** المستودع
> عامّ. مكانها متغيّرات Vercel وحدها. ومفتاح `service_role` بالذات
> يتجاوز عزل البيانات كليًّا — من يملكه يقرأ بيانات كل المعلّمين.

---

## ٥) ما لم يُنجَز بعد

- `SUPABASE_SERVICE_ROLE_KEY` و`ANTHROPIC_API_KEY` **لم تُضبَط في Vercel
  بعد** — فميزات الذكاء الاصطناعي وحذف الحساب معطّلة حتى تُضاف.
- مفاتيح مويسر لم تُضبط — فالاشتراك المدفوع معطّل.
- `AWARD_OPEN` و`AWARD_CLOSES_AT` لم تُضبطا — وبوّابة الجائزة **مغلقة
  افتراضيًّا عند غيابهما** (تصميم مقصود: تفشل مغلقة لا مفتوحة).
- **القسم السادس من `supabase/schema.sql`** (جدولا الجائزة) لم يُطبَّق
  على قاعدة البيانات بعد.
- خادم SCCC بالرياض لم يُجهَّز — وصفحة الخصوصية تَعِد بأن البيانات داخل
  المملكة، وهو وعدٌ سابق للواقع حتى يتمّ الترحيل.
- الفوتر في `index.html` يعرض النطاق `malafi.sa` بينما الموقع يعمل على
  `mlfe.vercel.app` — يُوحَّد عند ربط النطاق.
- «مساعد الجاهزية» (`rdCompute` · `rdTasks` · `rdRender`) موجود في
  `app-v25.html` ولم يُنقَل إلى البناء الحالي.

---

## ٦) مصائد تعلّمناها بالتجربة — اقرأها قبل أن تعدّل

**١. `const` قبل تعريفها توقف الملف كلّه بصمت.** الملف واحد وطويل؛ إن
قرأتَ `const` أو `let` قبل سطر تعريفها توقّف تنفيذ بقيّة الشيفرة بلا
رسالة خطأ ظاهرة — بينما تظلّ دوال `function` المرفوعة موجودة، فيبدو كأن
كل شيء سليم. إن تعطّل نصف التطبيق فجأة بلا سبب، ابحث عن هذا أولًا.

**٢. `letter-spacing` يُفسد العربية في PDF.** `html2canvas` يرسم كل حرف
على حدة متى وُجدت `letter-spacing`، فتنفصل الحروف عن بعضها. في نهاية
كتلة التنسيق قاعدة تفرض `letter-spacing:normal` على كل الوثائق — **لا
تُزلها.**

**٣. `\b` لا تعمل بعد كلمة عربية.** حدّ الكلمة في جافاسكربت معرَّف على
`[A-Za-z0-9_]` وحدها. اختبارٌ مثل `/^الوحدة\b/` لا يتحقّق أبدًا. استعمل
`(\s|:|$)` بدلًا منه.

**٤. للطباعة نظامان مختلفان.** ملف الإنجاز مبنيّ صفحاتٍ بمقاس A4 تحمل
تخطيطها بنفسها فتُطبع بحشو صفر؛ أمّا الوثائق المتدفّقة (`.doc` بلا
`.pf-doc`) فتحتاج هامش ١٢مم وإلّا قصّت الطابعة أطراف الأسطر. وارتفاع
الصفحة `min(294.5mm, calc(100vh - 1px))` — الحارس ضروريّ لأن تفعيل
«الرؤوس والتذييلات» يفرض هوامش تجعل كل صفحة تأخذ ورقتين.

**٥. الملف المنزَّل PDF لا يمرّ بقواعد الطباعة إطلاقًا.** `docToPdfBlob`
يلتقط ما على الشاشة، فحشو الجوّال الضيّق يصل معه. الدالة ترفع الحشو إلى
حدٍّ آمن وقت الالتقاط ثم تعيده.

**٦. كتب المنهج PDF تخزّن الحروف بأشكالها المتّصلة.** استخراج نصّها بلا
تطبيع `NFKC` يعطي كلامًا لا يُقرأ ولا يُطابَق. وpdf.js يسلّم قصاصات لا
أسطرًا، فالأسطر تُبنى من إحداثيات القصاصات مرتَّبةً من اليمين لليسار.

---

## ٧) كيف تتحقّق من عملك

لا توجد مجموعة اختبارات في المستودع. التحقّق كان يجري بتشغيل التطبيق في
متصفّح Chrome صامت واستدعاء معالجاته الحقيقية، ثم قياس الناتج: عدد
أوراق PDF ومقاسها، ونسبة ما تملؤه الصورة من الورقة، والخطوط المضمَّنة
داخل الملف الناتج.

وأقلّ ما ينبغي فعله يدويًّا بعد أي تعديل:

1. افتح `/app` وأنشئ حالة فيها بيانات — المتصفّح الفارغ يخفي أعطالًا
   يراها كل معلّم.
2. ولّد ملف الإنجاز، ونزّله PDF، وتحقّق أن الصفحات كاملة لا مصغّرة وأن
   **الملحق التفصيلي** في آخره لم يسقط.
3. اطبع من جوّال حقيقي — لا من محاكي سطح المكتب. أعطال الطباعة كلها
   تقريبًا ظهرت على الجوّال وحده.
4. جرّب المايك على أندرويد: كان يكرّر آخر ما قيل بعد كل وقفة.
