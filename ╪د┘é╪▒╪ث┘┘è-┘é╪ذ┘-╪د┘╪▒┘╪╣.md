# مَلَفّي — دليل الرفع (Vercel)

## قبل أي شيء: ماذا صلّحت في الكود
الملف الذي استلمته كان فيه **خطأ برمجي قاتل** — الدالتان `aiCall` و`aiCallVision` كان بعد إغلاقهما كود قديم متبقٍّ من نسخة سابقة (بقايا `try/catch` معلّقة تستدعي `return` خارج أي دالة). هذا يوقف تفسير الملف بالكامل عند المتصفح؛ التطبيق ما كان سيعمل إطلاقًا بعد الرفع، ليس فقط ميزات الذكاء الاصطناعي.
صلّحت الدالتين، وأزلت الكود الميت، وشغّلت `check.py` — كل الفحوصات الـ12 ناجحة.

## لماذا Vercel لا Hostinger
طلبت اختيار الاستضافة المناسبة. اخترت **Vercel** لأن التطبيق يحتاج:
- استضافة ملف HTML ثابت (سريع ومجاني عليها)
- **نقطة نهاية خادمية صغيرة** (`/api/chat`, `/api/vision`) لإخفاء مفتاح Anthropic

Hostinger مناسبة لمواقع PHP/ووردبريس التقليدية، وتتطلب خطوات إضافية (Node hosting منفصل) لتشغيل نقطة نهاية كهذه. Vercel تدعم الاثنين معًا من مجلد واحد، مجانًا، مع دومين ثابت (وليس رابطًا مؤقتًا) بمجرد إضافة مشروع.

## محتويات هذه الحزمة
| الملف | الدور |
|---|---|
| `index.html` | التطبيق بعد التصحيح — يستدعي `/api/chat` و`/api/vision` بدل استدعاء Anthropic مباشرة |
| `api/chat.js` | نقطة النهاية الوسيطة للنص — تحمل المفتاح خادميًا |
| `api/vision.js` | نقطة النهاية الوسيطة للصور |
| `package.json` | إعداد بسيط لتعريف بيئة Node |
| `vercel.json` | يمنح استدعاءات الذكاء الاصطناعي حتى 30 ثانية بدل المهلة الافتراضية 10 ثوانٍ |

## ⚠️ لم أستطع رفعه مباشرة بنفسي
ليس لدي اتصال إنترنت من بيئتي أو حساب Vercel خاص بك لأرفع نيابة عنك. جهّزت كل شيء جاهزًا للرفع؛ الخطوات التالية تأخذ خمس دقائق تقريبًا ولا تحتاج سطر أوامر.

## خطوات الرفع (بدون سطر أوامر)

### 1) أنشئ حساب Vercel
اذهب إلى vercel.com → Sign Up → سجّل بحساب جوجل أو GitHub.

### 2) ارفع المشروع
- الطريقة الأسهل: من لوحة Vercel اختر **Add New → Project**، ثم اسحب مجلد الحزمة هذا (الذي يحوي `index.html` و`api/`) في نافذة الرفع المباشر (Deploy without Git)، أو
- الطريقة الأفضل للتعديلات المستقبلية: ارفع هذا المجلد إلى مستودع GitHub جديد أولًا، ثم اربطه بـVercel عبر **Import Git Repository** — بهذا كل تعديل تدفعه لاحقًا يُنشر تلقائيًا.

### 3) أضف مفتاح Anthropic كمتغيّر بيئة (الخطوة الحرجة)
Project Settings → **Environment Variables** → أضف:
```
الاسم:  ANTHROPIC_API_KEY
القيمة: مفتاحك الفعلي من console.anthropic.com
```
ثم **Redeploy** (زر إعادة النشر) حتى يلتقط المتغيّر. بدون هذه الخطوة سترجع نقطتا النهاية رسالة "مفتاح غير مهيأ" وهي رسالة واضحة للمستخدم بدل الفشل الصامت.

### 4) دومين ثابت
Project Settings → **Domains**. Vercel يعطيك تلقائيًا رابطًا ثابتًا من نوع:
```
malaffi.vercel.app
```
وهذا **ليس رابطًا مؤقتًا** — يبقى ثابتًا طالما المشروع موجود (الروابط المؤقتة في Vercel هي روابط المعاينة لكل نشر، وهذا رابط الإنتاج). إن أردت دومينًا خاصًا بك (مثل malaffi.app) أضفه من نفس الصفحة واتبع تعليمات DNS الظاهرة هناك.

## بعد الرفع — تحقق بنفسك (كما طلبت)
1. افتح الرابط على جوالك.
2. جرّب أي ميزة ذكاء اصطناعي (مثلًا تصنيف شاهد بالصورة) وتأكد إنها ترجع نتيجة فعلية لا رسالة خطأ.
3. جرّب فصل الإنترنت لحظيًا وأعد المحاولة — يجب أن تظهر رسالة واضحة بدل التجمّد الصامت.
4. ولّد ملف إنجاز كامل، ثم اطبعه PDF (من متصفح الجوال: مشاركة → طباعة → حفظ كـPDF)، وتحقق أن الصفحات تنقسم صح على A4 خصوصًا صفحات الشواهد المتعددة (شبكة 2×2 / 2×3).

## المرحلة القادمة (كما اتفقنا، ليست الآن)
الحسابات، المزامنة، الدفع — تبقى للمرحلة التالية على Laravel. هذه الحزمة تحل فقط: (1) وسيط المفتاح (2) رسالة الخطأ الواضحة (3) رفع بدومين ثابت لتجربة معلّم واحد على جواله.
